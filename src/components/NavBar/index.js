import React from "react";
import {Container} from './styled'
//props.children
//todo lo que viene adentro de un elemento son sus hijos (Dom)
export default ({children}) => <Container>{children}</Container>